# LLM SPEC (não negociar)
- NUNCA remover o módulo de Tunnel nem suas rotas/menus.
- Tema padrão: **dark** (a paleta é a do arquivo `apps/client/src/styles/tokens.css`).
- Qualquer mudança de UI deve usar as **CSS variables** desses tokens; vedado hardcode de hex fora de `tokens.css`.
- A sala **não pode** ser perdida ao trocar para a aba Tunnel (estado da sala persiste em `useRoomStore`).
- Transferência P2P deve continuar funcional (PeerManager + TransferService).
- Se precisar tocar nesses arquivos, **só refatorar sem remover a feature**:
  - apps/client/src/pages/Tunnel.tsx (ou equivalente)
  - apps/client/src/components/PeersPanel.tsx
  - apps/client/src/state/useRoomStore.ts
  - apps/client/src/lib/rtc/PeerManager.ts
  - apps/client/src/lib/transfer/TransferService.ts
